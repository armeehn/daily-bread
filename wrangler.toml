# Cloudflare Pages config for the Daily Bread Studio (this `studio` branch).
# Only used if you deploy from the CLI (`npx wrangler pages deploy .`).
# The recommended path is the dashboard git integration — see CLOUDFLARE.md.
name = "daily-bread-studio"
pages_build_output_dir = "."
compatibility_date = "2024-01-01"
