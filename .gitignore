node_modules/
package.json
package-lock.json
.claude/

# wrangler / Cloudflare
.wrangler/
.dev.vars*
!.dev.vars.example

# newsproof — private signing keys never belong in the repo or on the server.
# The public keys live in .well-known/newsproof/ (committed); the seeds stay
# offline. Python bytecode caches are noise.
tools/newsproof/store/
__pycache__/
*.pyc

# rendered print PDFs (regenerate via db-render/serve-and-render.js)
db-render/out/
db-render/node_modules/

# the print document compiled from content/issue-*.js by tools/print/build-print.js
db-render/build-*.dc.html
